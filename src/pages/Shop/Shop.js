import React from 'react';
import './Shop.scss';

const Shop = () => {
  const [perfumes, setPerfumes] = useState([]);

  useEffect(() => {
    fetch('..');
  });

  return <div className="">Shop</div>;
};

export default Shop;
